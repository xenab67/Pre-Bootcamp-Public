#Create a function that accepts a number as input, return a list that counts down by one from the number inputted, down to zero. 
# def countDown(num):
#     newList = []
#     for i in range (num, -1, -1):
#         newList.append(i)
#     return newList
# print(countDown(10))

#Create a function that will receive a list with 2 numbers, print the first value and return the second. 
# def print_and_return(list):
#     print(list[0])
#     return list[1]
# print (print_and_return([5,67]))

#Create a function that accepts a list and returns the sum of the first value in the list plus the list's length.
def first_plus_length(list):
    return sum(first_plus_length[0], len(first_plus_length))
print(first_plus_length([10,9,4,3]))